import React from 'react';

const TestComponent = (props) => {
  // Access the 'level' property
  console.log("props", props); // This will log "A1", "A2", or any other level

  // Now, you can access the lessons and classes for this level from your data
  // For example, if you want the classes for this level:
  
};

export default TestComponent;
